# arm-bci-copilot — active pipeline dependencies.
# Python 3.12. Versions are the ones this repo is currently developed against;
# nothing here is version-sensitive, so >= is fine unless a run stops reproducing.
#
#   pip install -r requirements.txt
#
# The pre-2026 pip freeze from the RL era (gym / stable-baselines3 / wandb) moved
# to legacyRL/requirements_legacy.txt — it is only needed to run legacyRL/.

# ── required: everything in the root pipeline + studies/ ────────────────────
numpy==2.3.1
pandas==2.3.2
torch==2.6.0

# ── optional ────────────────────────────────────────────────────────────────
matplotlib==3.10.3    # studies/diagnose_learning.py learning-curve figures
openpyxl==3.1.5       # analysis/profile_sources.py .xlsx export

# ── optional: experimental/ (Problem 2, language decoding) ──────────────────
wordfreq==3.1.1       # word_decode.py, sentence_decode.py, prior_fusion_probe.py
anthropic==0.117.1    # sentence_rerank.py (needs ANTHROPIC_API_KEY; --mock runs without)
