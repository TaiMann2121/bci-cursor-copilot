# ── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/
env/

# ── Generated artifacts (all regenerable; nothing here is an input) ──────────
# train_copilot.py model runs (per-config, per-subject)
runs/
# studies/ + analysis/ output (json, html, figures)
results/
# blend_constructor.py output
data/blend/
# archive/surrogate_constructor.py output
data/surrogate/
# loose model checkpoints (models live under runs/)
*.pt

# ── Legacy RL run outputs (Lee / SJtools, under legacyRL/) ──────────────────
legacyRL/SJtools/copilot/runs/LAB_dataDriven_*/
legacyRL/SJtools/copilot/runs/lab_training/LAB_dataDriven_*/
legacyRL/SJtools/copilot/runs/LAB_realData_*/
legacyRL/SJtools/copilot/runs/dev/

# ── Caches ──────────────────────────────────────────────────────────────────
# LLM reranker responses (pay-once)
experimental/.rerank_cache.json

# ── Redundant raw archives ──────────────────────────────────────────────────
data/*.zip

# ── OS / editor ─────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
# Word lock/owner files (while a .docx is open)
~$*
# .vscode/                  # uncomment if you don't want editor settings tracked

# ── Optional: large source data (uncomment to keep out of git) ──────────────
# data/online_arm_trajectories.csv
# data/online_arm_trajectories_EEGK_simulation.csv
# data/OnlineArmTrajectoryEEGK/
